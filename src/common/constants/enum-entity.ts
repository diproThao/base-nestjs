export enum RoleType {
  USER = 'USER',
  ADMIN = 'ADMIN',
  CUSTOMER = 'CUSTOMER',
  PARTNER = 'PARTNER',
}

export enum EBullQueue {
  MAIL = 'Q::mail',
}

export enum EBullJob {
  SEND_EMAIL = 'sendMail',
}
